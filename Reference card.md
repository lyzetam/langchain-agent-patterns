  Quick Reference Card

  AGENTS (ask by name or describe the task)
    planner              → complex feature planning
    code-reviewer        → pre-PR review
    security-reviewer    → auth/data security audit
    python-reviewer      → FastAPI/Pydantic deep review
    go-reviewer          → Go idioms, concurrency, errors
    database-reviewer    → schema, migrations, N+1
    tdd-guide            → strict RED→GREEN→REFACTOR
    build-error-resolver → build/type/lint failures
    refactor-cleaner     → dead code, complexity
    doc-updater          → keep docs in sync
    loop-operator        → autonomous multi-cycle sessions
    harness-optimizer    → audit this entire library

  COMMANDS (/name to invoke)
    /learn               → extract patterns → skills/learned/
    /remember            → write instruction to rules permanently
    /checkpoint          → git stash/commit waypoint
    /loop                → start iterative loop session
    /orchestrate         → coordinate multiple agents
    /quality-gate        → binary PASS/FAIL before merge
    /compact             → save state then compact context
    /verify              → pre-commit verification checklist
    /wrap-session        → end-of-session cleanup + git checkpoint
    /self-reflect        → session review → update MEMORY.md

  RULES (always active, no invocation needed)
    essentials.md        → AWS SM checklist, self-correction protocol
    typescript/          → Next.js 14 App Router patterns
    python/              → FastAPI, Pydantic v2, async
    go/                  → error wrapping, context, concurrency
    kubernetes/          → FluxCD, ExternalSecrets, GitOps
    common/git.md        → conventional commits
    common/testing.md    → test pyramid, coverage standards