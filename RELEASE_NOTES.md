# Release Notes

_Generated by [`examples/release-notes-agent/agent.py`](examples/release-notes-agent/agent.py) run with `--repo <repo-root> --version 0.1.0` against this repo's git history (model: `anthropic:claude-sonnet-4-6`). This file is the agent's actual output — the repo dogfooding its own sample app._

## 🚀 Release 0.1.0

**Added**
- Introduced LangChain/LangGraph agent examples and supporting documentation.
- Added a Deep Agents sample app for writing release notes.

**Changed**
- Restructured the repository into `docs/`, `examples/`, and `outputs/` directories; added `--repo` mode for improved usability.
